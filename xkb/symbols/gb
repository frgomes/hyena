// Hyena keybindings
// A compact layout for typists, polyglots, mathematicians, programmers and Emacs users.
// (c) 2015 by Richard Gomes - http://github.com/frgomes/hyena

// For more infomation, please see:  xkb/symbols/hyena

partial alphanumeric_keys
xkb_symbols "hyena" {
    name[Group1]= "English (UK, Hyena Layer5)";
    include "gb(basic)"
    //NOTE:: should never: include "us(symbolic)"
    include "hyena(level5)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(bksp)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-dvorak" {
    name[Group1]= "Hyena Layer5 (Dvorak)";
    include "gb(dvorak)"
    //NOTE:: should never: include "us(symbolic)"
    include "hyena(level5)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-colemak" {
    name[Group1]= "English (UK, Hyena Layer5, Colemak)";
    include "gb(colemak)"
    //NOTE:: should never: include "us(symbolic)"
    include "hyena(level5)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-miniguru" {
    name[Group1]= "English (UK, Hyena Layer5, MiniGuru emulation)";
    include "gb(basic)"
    include "hyena(level5-miniguru)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-miniguru-intl" {
    name[Group1]= "English (UK, Hyena Layer5, MiniGuru emulation, intl., with dead keys)";
    include "gb(intl)"
    include "hyena(level5-miniguru)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-yoda" {
    name[Group1]= "English (UK, Hyena Layer5, TEX Yoda emulation)";
    include "gb(basic)"
    include "hyena(level5-yoda)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};

partial alphanumeric_keys
xkb_symbols "hyena-yoda-intl" {
    name[Group1]= "English (UK, Hyena Layer5, TEX Yoda emulation, intl., with dead keys)";
    include "us(alt-intl)"
    include "hyena(level5-yoda)"
    include "hyena(esc)"
    include "hyena(tab)"
    include "hyena(tlde)"
    include "level3(ralt_switch)"
    include "level5(caps_switch)"
    include "shift(both_shiftlock)"
};
